import 'package:flutter/material.dart';

class HockeyView extends StatelessWidget {
  const HockeyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
