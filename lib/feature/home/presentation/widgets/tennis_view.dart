import 'package:flutter/material.dart';

class TennisView extends StatelessWidget {
  const TennisView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
